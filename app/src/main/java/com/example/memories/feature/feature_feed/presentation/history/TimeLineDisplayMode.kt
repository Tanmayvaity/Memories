package com.example.memories.feature.feature_feed.presentation.history

enum class TimeLineDisplayMode(val displayName : String) {
    List(displayName = "List"),
    Calendar(displayName = "Calendar")
}